import turtle
from math import pi, sin, cos

def draw_heart(w, h, color, start_pos):
    t.pensize(2.5)
    t.up()
    t.goto(start_pos)  # Start at a specified position
    t.down()
    t.fillcolor(color)
    t.begin_fill()
